parameters:
  - name: hdf5tag
    default: hdf5-1_10_7
  - name: extraflags
    default: ''
  - name: gsltag
    default: gsl-2.7.1

steps:
  - bash: |
      curl -L https://github.com/facebook/zstd/releases/download/v1.5.2/zstd-v1.5.2-win64.zip > zstd.zip
      ls
      unzip zstd.zip
    displayName: Collect zstd
  
  - bash: |
      curl -L https://mirror.msys2.org/mingw/mingw64/mingw-w64-x86_64-gsl-${{ parameters.gsltag }}-1-any.pkg.tar.zst > ${{ parameters.gsltag }}.pkg.tar.zst
    displayName: Collect GSL